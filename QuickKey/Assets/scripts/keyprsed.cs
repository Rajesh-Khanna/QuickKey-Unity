using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using System;
public class keyprsed : MonoBehaviour {
	/*
	public Transform spawnPos;
	public GameObject spawnee;
	private GameObject gob;
	// Use this for initialization
	char c1, c2, c3;

	void Start () {
		gob = GameObject.Find ("gun");
	}
	
	// Update is called once per frame
	void Update () {
		c1 = managclas.LettersOnScreen [0];
		c2 = managclas.LettersOnScreen [1];
		c3 = managclas.LettersOnScreen [2];
		foreach(KeyCode kcode in Enum.GetValues(typeof(KeyCode)))
		{
			if (Input.GetKeyDown(kcode)) {
				if((char)kcode == Char.ToLower(c1))
					fire (1);
				else if((char)kcode == Char.ToLower(c2))
					fire (2);
				else if((char)kcode == Char.ToLower(c3))
					fire (3);
			}
		}
	}

	void fire(int loc){

		spawnPos = gob.transform;
		Debug.Log (gob.transform.eulerAngles.z);
		Instantiate (spawnee, spawnPos.position, spawnPos.rotation);
	}*/
}
